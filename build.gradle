plugins {
    id "org.sonarqube" version "3.5.0.2730"
}

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    // Dependencies go here
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com.dedup.Main'
        )
    }
}
